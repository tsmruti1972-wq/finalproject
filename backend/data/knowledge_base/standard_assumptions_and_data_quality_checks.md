# Standard Assumptions and Data Quality Checks

Validate that actual and budget files use consistent account mapping and period labels.

Check for missing values in key fields: account, cost center, period, actual, budget, prior.

Confirm sign conventions for revenue and expense are consistent across sources.

Document exchange rates and allocation methods when consolidating multi-entity data.

If any required field is missing, classify output as provisional.
